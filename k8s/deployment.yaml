apiVersion: apps/v1
kind: Deployment
metadata:
  name: astrons
  labels:
    app: astrons
spec:
  replicas: 1
  selector:
    matchLabels:
      app: astrons
  template:
    metadata:
      labels:
        app: astrons
    spec:
      containers:
        - name: astrons
          image: astrons:1.0
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          env:
            - name: PYTHONUNBUFFERED
              value: "1"
            - name: PULSAR_BROKER
              value: "pulsar-broker.pulsar.svc.cluster.local"
            - name: PULSAR_PORT
              value: "6650"
            - name: ASTRONS_EPOCH
              value: "2024-01-01T00:00:00.0z"
            - name: ASTRONS_MODEL_FILE
              value: "source/models/Simple/SimpleSensorCollectionModel.yml"
            - name: ASTRONS_END_TIME
              value: "86400"
          volumeMounts:
            - name: config-volume
              mountPath: /app/source/models/Simple
            - name: results-volume
              mountPath: /app/Results
            - name: cache-volume
              mountPath: /home/astrons/.cache
      volumes:
        - name: config-volume
          configMap:
            name: astrons-config
            items:
              - key: SimpleSensorCollectionModel.yml
                path: SimpleSensorCollectionModel.yml
        - name: results-volume
          persistentVolumeClaim:
            claimName: astrons-results-pvc
        - name: cache-volume
          persistentVolumeClaim:
            claimName: astrons-cache-pvc
---
# apiVersion: v1
# kind: Service
# metadata:
#   name: astrons
# spec:
#   selector:
#     app: astrons
#   ports:
#     - port: 6650
#       targetPort: 6650
#       nodePort: 30000
#   type: NodePort
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: astrons-results-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: astrons-cache-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
  storageClassName: local-path
